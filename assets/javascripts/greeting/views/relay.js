
var App = require('../app');
var PageView = require('../pageview');

App.Pages.Relay = new (PageView.extend({
    events: {},
    initPage: function() {},
    render: function() {}
}))({el: $('#view-relay')});
